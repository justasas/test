<%= form_for :account, :url => url_for(:controller => 'session', :action => 'create') do |f| %>
  <% if @account.errors.any? %>
  <div id="error explanation">
    <h2><%= pluralize(@account.errors.count, "error") %> prohibited
      this account from being saved:</h2>
    <ul>
    <% @account.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field(:name) %>
  </p>
  <p>
    <%= f.label :pass %><br>
    <%= f.text_field :pass %>
  </p>
  <p>
    <%= f.submit "Login"%>
  </p>
<% end %>
